#include<stdio.h>
void main()
{
    int n;
    printf("Check the no. is odd or even:");
    scanf("%d",&n);
    if(n%2==0)
    {
        printf("No. is even");
    }
    else
    {
        printf("No. is odd");
    }
}